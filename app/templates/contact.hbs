<ul>
  {{#each legislator in model}}
    <li>
      <span class="legislator-name">
        {{legislator.title}}
        {{legislator.first_name}}
        {{legislator.nickname}}
        {{legislator.last_name}}
        {{legislator.party}}
        {{legislator.state}}
      </span>
      <br>
      Contact: <a href={{legislator.contact_form}}>{{legislator.contact_form}}</a>
    </li>
  {{/each}}
</ul>
